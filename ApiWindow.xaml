<Window x:Class="BybitBot.ApiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BybitBot"
        mc:Ignorable="d"
        Style="{StaticResource WindowStyled}" Title="Api" Height="290" Width="400" ShowInTaskbar="False" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" ResizeMode="NoResize" Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock>Название</TextBlock>
        <TextBlock Grid.Row="1">API Key</TextBlock>
        <TextBlock Grid.Row="2">API Secret</TextBlock>
        <TextBlock Grid.Row="3">Комиссия</TextBlock>
        <TextBox Grid.Column="1" Name="apiName"></TextBox>
        <TextBox Grid.Column="1" Grid.Row="1" Name="apiKey"></TextBox>
        <TextBox Grid.Column="1" Grid.Row="2" Name="apiSecret"></TextBox>
        <TextBox Grid.Column="1" Grid.Row="3" Name="apiComission"></TextBox>
        <CheckBox Grid.Column="1" Grid.Row="5" Name="apiTest" Margin="5 0 0 0">testnet</CheckBox>
        <Button Grid.Row="6" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Button_Click">Сохранить</Button>
    </Grid>
</Window>
